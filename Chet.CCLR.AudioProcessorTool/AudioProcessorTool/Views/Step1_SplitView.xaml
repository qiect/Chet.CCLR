<UserControl x:Class="AudioProcessorTool.Views.Step1_SplitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:AudioProcessorTool.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 文件设置区域 -->
        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="📁 选择文本文件" Width="140" Click="BtnSelectFile_Click" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtFilePath" VerticalAlignment="Center" Width="500" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="10,0,0,0" Foreground="#64748B"/>
                <Button Content="🚀 自动分句" Width="120" Margin="20,0,0,0" Click="BtnSplit_Click"/>
            </StackPanel>
        </Border>
        
        <!-- 分句结果区域 -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                
                <ListView x:Name="LvSentences" Grid.Column="0" ItemsSource="{Binding Sentences}" BorderBrush="#E2E8F0" BorderThickness="1" Background="White" Margin="0,0,20,0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="序号" Width="80" DisplayMemberBinding="{Binding Index}"/>
                            <GridViewColumn Header="句子内容" Width="Auto" DisplayMemberBinding="{Binding}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                
                <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="💡 操作指南" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,15" Foreground="#1E293B"/>
                        <TextBlock Text="1. 选择文本文件 (.txt)" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="2. 点击自动分句按钮" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="3. 查看分句结果" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="4. 导出 sentences.txt" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <Button Content="📊 导出结果" Width="140" Margin="0,15,0,0" Click="BtnExport_Click"/>
                        <Button Content="👁️ 预览内容" Width="140" Margin="0,10,0,0" Click="BtnPreview_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="状态:" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtStatus" Text="就绪" Foreground="#64748B"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>