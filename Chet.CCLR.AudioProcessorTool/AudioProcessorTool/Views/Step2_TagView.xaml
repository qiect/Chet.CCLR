<UserControl x:Class="AudioProcessorTool.Views.Step2_TagView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:AudioProcessorTool.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 文件设置区域 -->
        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="📁 选择音频文件" Width="140" Click="BtnSelectAudio_Click" Margin="0,0,10,0"/>
                <Button Content="📂 加载" Width="90" Click="BtnLoad_Click" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtAudioPath" VerticalAlignment="Center" Width="500" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="10,0,0,0" Foreground="#64748B"/>
            </StackPanel>
        </Border>
        
        <!-- 控制面板 -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,30,0">
                    <Button x:Name="BtnPlayPause" Content="▶ 播放/暂停" Width="130" Click="BtnPlayPauseInternal_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnStop" Content="⏹ 停止" Width="90" Click="BtnStopInternal_Click"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BtnAddTag" Content="➕ 添加标签" Width="120" Click="BtnAddTag_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnDeleteTag" Content="🗑️ 删除标签" Width="120" Click="BtnDeleteTag_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnExport" Content="💾 导出标签" Width="120" Click="BtnExport_Click"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="TxtCurrentTime" FontSize="14" Width="140" Margin="0,0,20,0" TextAlignment="Right" Foreground="#64748B"/>
                    <TextBlock x:Name="TxtTotalTime" FontSize="14" Width="140" TextAlignment="Right" Foreground="#64748B"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 标签列表 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>
                
                <ListView x:Name="LvTags" Grid.Column="0" ItemsSource="{Binding Tags}" SelectionMode="Single" SelectionChanged="LvTags_SelectionChanged" BorderBrush="#E2E8F0" BorderThickness="1" Background="White" Margin="0,0,20,0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="序号" Width="70" DisplayMemberBinding="{Binding Index}"/>
                            <GridViewColumn Header="开始时间" Width="110" DisplayMemberBinding="{Binding StartTime, StringFormat=\{0:0.000\}}"/>
                            <GridViewColumn Header="结束时间" Width="110" DisplayMemberBinding="{Binding EndTime, StringFormat=\{0:0.000\}}"/>
                            <GridViewColumn Header="时长" Width="80" DisplayMemberBinding="{Binding Duration, StringFormat=\{0:0.000\}}"/>
                            <GridViewColumn Header="操作" Width="90">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="▶" Width="60" Margin="2,0" Click="BtnPlayTag_Click" CommandParameter="{Binding}" ToolTip="播放该时间段"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                
                <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="💡 操作指南" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,15" Foreground="#1E293B"/>
                        <TextBlock Text="1. 选择音频文件并加载" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="2. 播放音频，按空格键添加标签" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="3. 点击▶试听该时间段" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="4. 删除不需要的标签" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                        <TextBlock Text="5. 导出标签文件" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="#64748B" LineHeight="18"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="状态:" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtStatus" Text="就绪 - 请选择音频文件" Foreground="#64748B"/>
            </StackPanel>
        </Border>
        
        <!-- 进度条 -->
        <Border Grid.Row="4" Background="White" CornerRadius="8" Padding="20" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="⏳" Grid.Column="0" Margin="0,0,15,0" VerticalAlignment="Center" Foreground="#64748B"/>
                <Slider x:Name="SliTimeline" Grid.Column="1" Minimum="0" Maximum="100" ValueChanged="SliTimeline_ValueChanged" PreviewMouseUp="SliTimeline_PreviewMouseUp" Height="30" Background="#E2E8F0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>